# Render Blueprint: https://render.com/docs/infrastructure-as-code
# Если деплой падает на создании БД: на бесплатном плане у аккаунта только одна Free Postgres —
# удали старую или задай другое имя ресурса в Dashboard.

databases:
  - name: jobflex-db
    databaseName: jobflex
    user: jobflex
    plan: free

services:
  # region не указываем: после создания сервиса Render не даёт менять регион через Blueprint sync.
  - type: web
    name: jobflex
    runtime: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: python manage.py migrate --noinput && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 4 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: DJANGO_DEBUG
        value: "false"
      - key: DJANGO_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: jobflex-db
          property: connectionString
      - key: DJANGO_ALLOWED_HOSTS
        value: jobflex-4fh0.onrender.com
      - key: DJANGO_CSRF_TRUSTED_ORIGINS
        value: https://jobflex-4fh0.onrender.com
      - key: SITE_URL
        value: https://jobflex-4fh0.onrender.com
